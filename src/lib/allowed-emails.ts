/**
 * Allowlist of email addresses permitted to access the admin panel.
 * Add staff emails here before onboarding them.
 * Email comparison is always lowercase — no case sensitivity issues.
 */
export const ALLOWED_ADMIN_EMAILS: string[] = [
  'renewshinedmv@gmail.com',
]
